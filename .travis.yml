language: rust

rust:
    - 1.0.0-beta.2
    - nightly

sudo: false

notifications:
    email:
        on_success: never
        on_failure: always

script:
    - '[ "$TRAVIS_RUST_VERSION" = "1.0.0-beta.2" ] || cargo build --features no_std'
    - '[ "$TRAVIS_RUST_VERSION" = "1.0.0-beta.2" ] || cargo test --features no_std'
    - cargo build
    - cargo test
    - cargo doc --no-deps
    - rustdoc --test README.md -L target/debug

after_success:
    - curl https://mvdnes.github.io/rust-docs/travis-doc-upload.sh | sh
